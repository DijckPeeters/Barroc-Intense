<Page
    x:Class="Barroc_Intense.Pages.NewDeliveryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="Yellow" Offset="0.0"/>
                <GradientStop Color="Black" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="#AAFFFFFF"
                CornerRadius="25"
                Padding="40"
                Margin="40"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="15" Width="400" HorizontalAlignment="Center">

                    <TextBlock Text="Nieuwe Levering Toevoegen"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="Black"
                               HorizontalAlignment="Center"/>

                    <TextBox x:Name="newProductId" Header="Product ID" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newOrderId" Header="Order ID" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newCustomerId" Header="Customer ID" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newCustomerContact" Header="TelefoonNummert" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newProductName" Header="Product Naam" Padding="8" CornerRadius="10" IsReadOnly="True"/>



                    <TextBox x:Name="newQuantity" Header="Aantal geleverd" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newExpectedQuantity" Header="Aantal verwacht" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newCustomerName" Header="Klantnaam" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newAddress" Header="Afleveradres" Padding="8" CornerRadius="10"/>
                    <DatePicker x:Name="newPlannedDate" Header="Geplande leveringsdatum"/>
                    <TextBox x:Name="newStatus" Header="Status" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newCarrier" Header="Vervoerder" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newDriver" Header="Naam chauffeur" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newTrackingNumber" Header="Trackingnummer" Padding="8" CornerRadius="10"/>
                    <TextBox x:Name="newNotes" Header="Opmerkingen" Padding="8" CornerRadius="10"/>

                    <TextBlock x:Name="validationResultsTextBlock"
                               Foreground="Red"
                               TextWrapping="Wrap"
                               FontSize="14"/>

                    <Button Content="Opslaan"
                            Click="SaveButton_Click"
                            Background="Black"
                            Foreground="Yellow"
                            FontWeight="Bold"
                            Height="45"
                            CornerRadius="10"
                            HorizontalAlignment="Center"/>
                    <Button Content="Terug naar Overzicht"
        Click="BackButton_Click"
        Background="Gray"
        Foreground="White"
        FontWeight="Bold"
        Height="45"
        CornerRadius="10"
        HorizontalAlignment="Center"/>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
