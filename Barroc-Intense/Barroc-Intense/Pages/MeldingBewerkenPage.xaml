<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Barroc_Intense.MeldingBewerkenPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>

        <!-- BACKGROUND -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFD700" Offset="0"/>
                <GradientStop Color="#000000" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40"
                        Spacing="28"
                        HorizontalAlignment="Center"
                        MaxWidth="900">

                <!-- PAGE TITLE -->
                <Border Background="#CC000000"
                        CornerRadius="18"
                        Padding="20">

                    <TextBlock Text="Melding aanpassen"
                               FontSize="38"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                </Border>
                <StackPanel Spacing="14">

                    <TextBlock Text="Basisgegevens"
               FontSize="28"
               FontWeight="Bold"/>

                    <Border Height="1"
            Background="#FFD700"
            Opacity="0.6"/>

                    <Grid ColumnSpacing="20" RowSpacing="14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBox x:Name="AfdelingBox"
                 Header="Afdeling"
                 Grid.Row="0"
                 Grid.Column="0"/>

                        <TextBox x:Name="MonteurBox"
                 Header="Monteur"
                 Grid.Row="0"
                 Grid.Column="1"/>

                        <TextBox x:Name="KlantBox"
                 Header="Klant"
                 Grid.Row="1"
                 Grid.Column="0"/>

                        <TextBox x:Name="ProductBox"
                 Header="Product"
                 Grid.Row="1"
                 Grid.Column="1"/>
                    </Grid>
                </StackPanel>

                <Border Background="White"
                        CornerRadius="22"
                        Padding="24">

                    <StackPanel Spacing="14">

                        <TextBlock Text="Probleem en status"
                                   FontSize="28"
                                   FontWeight="Bold"/>

                        <Border Height="1"
                                Background="#FFD700"
                                Opacity="0.6"/>

                        <TextBox x:Name="ProbleemBox"
                                 Header="Probleemomschrijving"
                                 Height="160"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"/>

                        <ComboBox x:Name="StatusCombo"
                                  Header="Status">
                            <ComboBoxItem Content="Open"/>
                            <ComboBoxItem Content="In behandeling"/>
                            <ComboBoxItem Content="Gesloten"/>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <!-- DATUM & TIJD -->
                <Border Background="White"
                        CornerRadius="22"
                        Padding="24">

                    <StackPanel Spacing="14">

                        <TextBlock Text="Planning"
                                   FontSize="28"
                                   FontWeight="Bold"/>

                        <Border Height="1"
                                Background="#FFD700"
                                Opacity="0.6"/>

                        <Grid ColumnSpacing="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <DatePicker x:Name="DatumPicker"
                                        Header="Datum"/>

                            <TimePicker x:Name="TijdPicker"
                                        Header="Tijd"
                                        Grid.Column="1"
                                        ClockIdentifier="24HourClock"
                                        MinuteIncrement="5"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- OPSLAAN -->
                <Border Background="White"
                        CornerRadius="22"
                        Padding="24">

                    <StackPanel HorizontalAlignment="Center"
                                Spacing="10">

                        <Button Content="Opslaan"
                                Width="260"
                                Height="60"
                                Background="#000000"
                                Foreground="#FFD700"
                                FontSize="20"
                                FontWeight="Bold"
                                Click="Opslaan_Click"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
