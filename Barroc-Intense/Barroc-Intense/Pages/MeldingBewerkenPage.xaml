<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Barroc_Intense.MeldingBewerkenPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Barroc_Intense"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">



        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="Yellow" Offset="0.0"/>
                    <GradientStop Color="Black" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Border Background="#AAFFFFFF" CornerRadius="10" Margin="20">
                <ScrollViewer>
                    <StackPanel Spacing="15" Margin="30">

                        <TextBlock Text="Melding aanpassen"
                               FontSize="48"
                               FontWeight="Bold"
                               Foreground="Yellow"
                               HorizontalAlignment="Center"/>

                        <TextBox x:Name="AfdelingBox" Header="Afdeling"/>
                        <TextBox x:Name="MonteurBox" Header="Monteur"/>
                        <TextBox x:Name="KlantBox" Header="Klant"/>
                        <TextBox x:Name="ProductBox" Header="Product"/>
                        <TextBox x:Name="ProbleemBox" Header="Probleemomschrijving" Height="200" AcceptsReturn="True"/>

                        <ComboBox x:Name="StatusCombo" Header="Status">
                            <ComboBoxItem Content="Open"/>
                            <ComboBoxItem Content="In behandeling"/>
                            <ComboBoxItem Content="Gesloten"/>
                        </ComboBox>
                    <DatePicker x:Name="DatumPicker"
            Header="Datum"
            HorizontalAlignment="Stretch"/>

                    <TimePicker x:Name="TijdPicker"
            Header="Tijd"
            ClockIdentifier="24HourClock"
            MinuteIncrement="5"
            HorizontalAlignment="Stretch"/>

                    <Button Content="Opslaan"
                            Background="Black"
                            Foreground="Yellow"
                            Click="Opslaan_Click"
                            FontWeight="Bold" FontSize="20"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Page>

