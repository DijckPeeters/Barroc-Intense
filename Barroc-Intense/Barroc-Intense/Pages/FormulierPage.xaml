<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Barroc_Intense.Pages.FormulierPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Barroc_Intense.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Barroc_Intense.Data"
    mc:Ignorable="d">

    <Grid>

        <!-- BACKGROUND -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFD700" Offset="0"/>
                <GradientStop Color="#000000" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40" Spacing="28" HorizontalAlignment="Center" MaxWidth="1000">

                <!-- PAGE TITLE -->
                <Border Background="#CC000000"
                        CornerRadius="18"
                        Padding="20">

                    <TextBlock x:Name="TitleBlock"
                               FontSize="38"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- KEURING -->
                <Border Background="White" CornerRadius="22" Padding="24">
                    <StackPanel Spacing="14">

                        <TextBlock Text="Keuring"
                                   FontSize="30"
                                   FontWeight="Bold"/>

                        <Border Height="1" Background="#FFD700" Opacity="0.6"/>

                        <CheckBox x:Name="ChecklistBox"
                                  Content="Checklist volledig uitgevoerd"/>

                        <CheckBox x:Name="GoedgekeurdBox"
                                  Content="Goedgekeurd"/>

                        <TextBox x:Name="KeuringOpmerkingenBox"
                                 Header="Opmerkingen"
                                 Height="140"
                                 TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- MELDING INFO -->
                <Border Background="White" CornerRadius="22" Padding="24">
                    <StackPanel Spacing="14">

                        <TextBlock Text="Melding"
                                   FontSize="30"
                                   FontWeight="Bold"/>

                        <Border Height="1" Background="#FFD700" Opacity="0.6"/>

                        <Grid ColumnSpacing="20" RowSpacing="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBox x:Name="KlantBox"
                                     Header="Klant (automatisch)"
                                     IsReadOnly="True"/>

                            <TextBox x:Name="MachineBox"
                                     Header="Type koffiemachine"
                                     Grid.Column="1"
                                     IsReadOnly="True"/>

                            <TextBox x:Name="InitieleBox"
                                     Header="Initiële melding"
                                     Grid.Row="1"
                                     AcceptsReturn="True"
                                     Height="100"/>

                            <TextBox x:Name="StoringscodeBox"
                                     Header="Storingscode"
                                     Grid.Column="1"
                                     Grid.Row="1"/>

                            <CheckBox x:Name="VerholpenBox"
                                      Content="Storing verholpen"
                                      Grid.Row="2"/>

                        </Grid>

                        <TextBox x:Name="VervolgBox"
                                 Header="Vervolgafspraken"
                                 Height="120"
                                 TextWrapping="Wrap"/>

                        <TextBox x:Name="BeschrijvingBox"
                                 Header="Korte beschrijving"
                                 Height="120"
                                 TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- ONDERDELEN -->
                <Border Background="White" CornerRadius="22" Padding="24">
                    <StackPanel Spacing="14">

                        <TextBlock Text="Onderdelen"
                                   FontSize="30"
                                   FontWeight="Bold"/>

                        <Border Height="1" Background="#FFD700" Opacity="0.6"/>

                        <ListView x:Name="OnderdelenList"
                                  SelectionMode="None">

                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="data:MaterialViewModel">
                                    <Grid ColumnDefinitions="Auto,Auto,Auto,Auto"
                                          ColumnSpacing="12"
                                          VerticalAlignment="Center">

                                        <CheckBox Content="{x:Bind Name}"
                                                  IsChecked="{x:Bind IsSelected, Mode=TwoWay}"/>

                                        <TextBlock Text="{x:Bind Quantity}"
                                                   Grid.Column="1"
                                                   Width="40"
                                                   TextAlignment="Center"
                                                   FontWeight="Bold"/>

                                        <Button Content="+"
                                                Grid.Column="2"
                                                Width="40"
                                                Click="Plus_Click"
                                                Tag="{x:Bind}"/>

                                        <Button Content="-"
                                                Grid.Column="3"
                                                Width="40"
                                                Click="Min_Click"
                                                Tag="{x:Bind}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- HANDTEKENING -->
                <Border Background="White" CornerRadius="22" Padding="24">
                    <StackPanel Spacing="14">

                        <TextBlock Text="Handtekening"
                                   FontSize="30"
                                   FontWeight="Bold"/>

                        <Border Height="1" Background="#FFD700" Opacity="0.6"/>

                        <Grid Width="520" Height="220">
                            <Canvas x:Name="SignatureCanvas"
                                    Background="#F9F9F9"
                                    PointerPressed="SignatureCanvas_PointerPressed"
                                    PointerMoved="SignatureCanvas_PointerMoved"
                                    PointerReleased="SignatureCanvas_PointerReleased"/>

                            <Image x:Name="SavedSignatureImage"
                                   Visibility="Collapsed"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Spacing="14">

                            <Button Content="Clear"
                                    Width="140"
                                    Background="Black"
                                    Foreground="Yellow"
                                    Click="ClearButton_Click"/>

                            <Button Content="Opslaan"
                                    Width="220"
                                    Height="56"
                                    Background="#FFD700"
                                    Foreground="Black"
                                    FontWeight="Bold"
                                    Click="Opslaan_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
